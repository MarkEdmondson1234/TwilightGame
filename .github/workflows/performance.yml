name: Performance Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel in-progress runs for the same branch
concurrency:
  group: perf-${{ github.ref }}
  cancel-in-progress: true

jobs:
  performance:
    runs-on: ubuntu-latest
    timeout-minutes: 20  # Increased for multi-map and device-throttled tests

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Puppeteer browser
        run: npx puppeteer browsers install chrome

      - name: Start dev server
        run: |
          npm run dev &
          # Wait for server to be ready
          npx wait-on http://localhost:4000/TwilightGame/ --timeout 60000

      - name: Download baseline (if exists)
        uses: actions/download-artifact@v4
        with:
          name: perf-baseline
          path: .
        continue-on-error: true

      - name: Run performance tests
        id: perf
        run: |
          # Run test on village map with movement scenario (baseline)
          echo "## Testing village map (baseline)..."
          npm run perf:ci || true

          # Compare against baseline if it exists
          if [ -f perf-baseline.json ]; then
            echo "baseline_exists=true" >> $GITHUB_OUTPUT
            node scripts/perf-test.js --map village --scenario movement --duration 15000 --compare perf-baseline.json --github || true
          else
            echo "baseline_exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Run multi-map tests
        run: |
          echo "## Testing bear_cave map..."
          node scripts/perf-test.js --map bear_cave --scenario explore --duration 10000 --github --save perf-bear.json || true

          echo "## Testing witch_hut map..."
          node scripts/perf-test.js --map witch_hut --scenario explore --duration 10000 --github --save perf-witch.json || true

      - name: Run device-throttled tests (simulated old iPad)
        run: |
          echo "## Testing village on simulated iPad Air 2 (4x CPU throttle)..."
          node scripts/perf-test.js --device ipad --map village --scenario movement --duration 10000 --github --save perf-ipad-village.json || true

          echo "## Testing bear_cave on simulated iPad 4th Gen (6x CPU throttle)..."
          node scripts/perf-test.js --device ipad-old --map bear_cave --scenario explore --duration 10000 --github --save perf-ipad-bear.json || true

          echo "## Testing witch_hut on simulated iPad 4th Gen (6x CPU throttle)..."
          node scripts/perf-test.js --device ipad-old --map witch_hut --scenario explore --duration 10000 --github --save perf-ipad-witch.json || true

      - name: Generate performance report
        id: report
        run: node scripts/perf-report.js

      - name: Comment on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const report = fs.readFileSync('perf-report.md', 'utf8');

            // Find existing comment
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const botComment = comments.find(c =>
              c.user.type === 'Bot' &&
              c.body.includes('## Performance Test Results')
            );

            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: report,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: report,
              });
            }

      - name: Upload results as artifact
        uses: actions/upload-artifact@v4
        with:
          name: perf-results
          path: |
            perf-results.json
            perf-bear.json
            perf-witch.json
            perf-ipad-*.json
            perf-screenshot.png
          retention-days: 30

      - name: Update baseline on main
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        uses: actions/upload-artifact@v4
        with:
          name: perf-baseline
          path: perf-results.json
          retention-days: 90

      - name: Check for regressions
        run: |
          if [ -f regression-detected ]; then
            echo "::error::Performance regression detected! Check the PR comment for details."
            exit 1
          fi
