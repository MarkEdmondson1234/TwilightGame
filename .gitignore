# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Claude Code git worktrees — checkouts of other branches, never part of a commit
.claude/worktrees/

# MCP server config — holds a literal Sentry access token on configured machines.
# Template: .mcp.json.example · Setup: .claude/skills/setup-sentry-mcp
.mcp.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
perf-screenshot.png
perf-*.json
perf-report.md
regression-detected
# Local output of scripts/art-review.mjs (contact sheets are attached to PRs, not committed)
art-review/
