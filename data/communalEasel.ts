/**
 * The communal easel's tile: where Draw and Craft Workshop open, and where the
 * kitchen draws the easel prop. One definition so the artwork and the two
 * activities cannot drift apart.
 */
export const COMMUNAL_EASEL = { mapId: 'mums_kitchen', x: 10, y: 5 } as const;
